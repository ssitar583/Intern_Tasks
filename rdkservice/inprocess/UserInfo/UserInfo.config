set(autostart ${PLUGIN_USERINFO_AUTOSTART})

if(PLUGIN_USERINFO_STARTUPORDER)
set (startuporder ${PLUGIN_USERINFO_STARTUPORDER})
endif()

map()
    key(root)
    map()
        kv(mode ${PLUGIN_USERINFO_MODE})
    end()
end()
ans(configuration)
