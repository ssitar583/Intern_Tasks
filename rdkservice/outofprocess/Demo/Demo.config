set(autostart ${PLUGIN_DEMO_AUTOSTART})
set (preconditions Platform)
set (callsign "Demo")

if(PLUGIN_DEMO_STARTUPORDER)
set (startuporder ${PLUGIN_DEMO_STARTUPORDER})
endif()

map()
    key(root)
    map()
        kv(mode ${PLUGIN_DEMO_MODE})
    end()
end()
ans(configuration)


